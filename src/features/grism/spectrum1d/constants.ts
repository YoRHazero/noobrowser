export const SQRT_2_LN2 = Math.sqrt(2 * Math.log(2));

export const DEFAULT_HRATIO = { top: 0.15, bottom: 0.8, gap: 0.05 };
export const DEFAULT_MARGIN = { top: 20, bottom: 50, left: 100, right: 30 };
